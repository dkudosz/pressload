export default function AboutPage() {
  return (
    <section>
      <h1 className="text-3xl font-bold text-foreground">About Pressload</h1>
      <p className="text-muted-foreground leading-relaxed mt-4 max-w-2xl">
        This route exists to demonstrate App Router folder-based navigation. Build your project
        sections by adding more folders inside the <code className="bg-muted px-1.5 py-0.5 rounded text-sm">app</code> directory.
      </p>
    </section>
  )
}
